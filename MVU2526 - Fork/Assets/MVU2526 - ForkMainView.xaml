<UserControl
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    mc:Ignorable="d"
    x:Class="MVU2526___Fork.MVU2526___ForkMainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="MVU2526"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style TargetType="Border" x:Key="PanelDefinition">
            <Setter Property="BorderThickness" Value="5" />
            <Setter Property="BorderBrush" Value="Red" />
            <Setter Property="CornerRadius" Value="5" />
        </Style>
    </UserControl.Resources>

    <Grid
        Background="Green"
        HorizontalAlignment="Left"
        VerticalAlignment="Center">
        <!-- <Image Opacity=".1" Source="Texture/UI/References/MainMenuReference.jpg" /> -->

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Style="{StaticResource PanelDefinition}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="50" />
                    </Grid.RowDefinitions>
                    <Grid
                        Background="Black"
                        Grid.RowSpan="2"
                        Opacity=".1" />
                    <Border Grid.Row="0" Style="{StaticResource PanelDefinition}">

                        <!--  Menu  -->
                        <Grid
                            Background="Green"
                            Margin="50,100,0,0"
                            Opacity=".5">
                            <Border Grid.Row="1" Style="{StaticResource PanelDefinition}">
                                <StackPanel>
                                    <StackPanel.Resources>
                                        <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                                            <Setter Property="HorizontalAlignment" Value="Left" />
                                            <Setter Property="Margin" Value="10,2,10,2" />
                                            <Setter Property="Padding" Value="10,2,10,2" />
                                            <Setter Property="Background" Value="Transparent" />
                                        </Style>
                                    </StackPanel.Resources>

                                    <Image Source="Texture/UI/References/Title.jpg" />

                                    <StackPanel Margin="20,20,20,20">
                                        <Button Content="Continue" />
                                        <Button Content="Load Game" />
                                        <Button Content="New Game" />
                                        <Button Command="{Binding IncrementNumber}" Content="Increment Number" />
                                        <TextBlock Foreground="White">
                                            <Run Text="Number:" /> <Run Text="{Binding Number}" />
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                        </Grid>

                    </Border>
                    <Border Grid.Row="1" Style="{StaticResource PanelDefinition}" />
                </Grid>
            </Border>

            <local:Popup Visibility="Collapsed" />
        </Grid>


        <!-- <StackPanel> -->
        <!--    -->
        <!--   <Rectangle Fill="Red" Width="100" Height="100" Margin="10 10 10 10"/> -->
        <!--    -->
        <!--  <Border Width="300" Background="DodgerBlue"  -->
        <!--  HorizontalAlignment="Center" VerticalAlignment="Center" Padding="50,20">  -->
        <!--   <StackPanel> -->
        <!--     <TextBlock x:Name="myText" Text="Épico" FontSize="30" Foreground="White"/> -->
        <!--    -->
        <!--     <Rectangle Height="50"/> -->
        <!--    -->
        <!--     <Button Content="Botonaso" Margin="0,0,0,0"/> -->
        <!--    -->
        <!--     <Rectangle Height="50"/> -->
        <!--    -->
        <!--     <Rectangle Height="50"> -->
        <!--       <Rectangle.Fill> -->
        <!--         <LinearGradientBrush StartPoint="0,0" EndPoint="1,0"> -->
        <!--           <GradientStop Color="Yellow" Offset="0.0" /> -->
        <!--           <GradientStop Color="Red" Offset="0.25" /> -->
        <!--           <GradientStop Color="Blue" Offset="0.75" /> -->
        <!--           <GradientStop Color="LimeGreen" Offset="1.0" /> -->
        <!--         </LinearGradientBrush> -->
        <!--       </Rectangle.Fill> -->
        <!--     </Rectangle> -->
        <!--   </StackPanel> -->
        <!-- </Border> -->
        <!-- </StackPanel> -->
    </Grid>
</UserControl>
